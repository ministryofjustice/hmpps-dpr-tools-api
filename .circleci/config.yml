version: 2.1

orbs:
  hmpps: ministryofjustice/hmpps@11
  slack: circleci/slack@4.12.1
  mem: circleci/rememborb@0.0.2

parameters:
  alerts-slack-channel:
    type: string
    default: dpr_cicd_alerts
  releases-slack-channel:
    type: string
    default: dpr_cicd_approvals
  workingdir:
    type: string
    default: '.'
  dockerfile_dir:
    type: string
    default: "."

jobs:
  validate:
    executor:
      name: hmpps/java
      tag: "21.0"
    steps:
      - setup_remote_docker
      - checkout
      - restore_cache:
          keys:
            - gradle-{{ checksum "build.gradle.kts" }}
            - gradle-
      - run:
          command: ./gradlew check
      - save_cache:
          paths:
            - ~/.gradle
          key: gradle-{{ checksum "build.gradle.kts" }}
      - store_test_results:
          path: build/test-results
      - store_artifacts:
          path: build/reports/tests
      - persist_to_workspace:
          root: << pipeline.parameters.workingdir >>
          paths:
            - .     

  build_multiplatform_docker_job:
    docker:
      - image: cimg/base:stable
    resource_class: small
    parameters:
      image_name:
        type: string
        default: "quay.io/hmpps/${CIRCLE_PROJECT_REPONAME}"
    steps:   
      - attach_workspace:
          at: .    
      - setup_remote_docker:
          docker_layer_caching: true
      - mem/remember:
          env_var: APP_VERSION
          value: "Build.${CIRCLE_SHA1}"
      - run:
          name: Create IMAGE_NAME env var
          command: |
            IMAGE_NAME="<< parameters.image_name >>"
            echo "export IMAGE_NAME=$IMAGE_NAME" >> $BASH_ENV
      - mem/remember:
          env_var: IMAGE_NAME
          value: "${IMAGE_NAME}"
      - run:
          name: Setup buildx
          command: |
            docker context create multi-arch-build
            docker run --rm --privileged multiarch/qemu-user-static --reset -p yes
            docker run --rm --privileged tonistiigi/binfmt --install all
            docker buildx create --use multi-arch-build --platform linux/arm64,linux/amd64
      - run:
          name: quay.io login
          command: docker login -u="${QUAYIO_USERNAME}" -p="${QUAYIO_PASSWORD}" quay.io            
      - run:
          name: Build container image
          command: |
            docker buildx build \
              --platform linux/amd64,linux/arm64 --pull \
              --progress plain \
              --rm=false << pipeline.parameters.dockerfile_dir >> \
              --build-arg BUILD_NUMBER=$APP_VERSION \
              --build-arg GIT_REF=$CIRCLE_SHA1 \
              --build-arg GIT_BRANCH=$CIRCLE_BRANCH \
              --tag "${IMAGE_NAME}:${APP_VERSION}" \
              --tag "${IMAGE_NAME}:latest" \
              --label "maintainer=dps-hmpps@digital.justice.gov.uk" \
              --label "app.version=${APP_VERSION}" \
              --label "build.version=${APP_VERSION}" \
              --label "build.number=${CIRCLE_BUILD_NUM}" \
              --label "build.url=${CIRCLE_BUILD_URL}" \
              --label "build.gitref=${CIRCLE_SHA1}" \
              --push 

  remember-app-version:
    executor:
      name: hmpps/java
      tag: "21.0"
    steps:
      - mem/remember:
          env_var: APP_VERSION
          value: "Build.${CIRCLE_SHA1}"

workflows:
  version: 2
  build-test-and-deploy:
    jobs:
      - validate:
          filters:
            tags:
              ignore: /.*/
      - hmpps/helm_lint:
          name: helm_lint
      - hmpps/helm_lint:
          name: helm_lint_probation
          chart_name: hmpps-dpr-tools-api-probation
          env: probation-dev
      - hmpps/helm_lint:
          name: helm_lint_probation_test
          chart_name: hmpps-dpr-tools-api-probation
          env: probation-test
      - hmpps/helm_lint:
          name: helm_lint_probation_preprod
          chart_name: hmpps-dpr-tools-api-probation
          env: probation-preprod
      - hmpps/helm_lint:
          name: helm_lint_probation_prod
          chart_name: hmpps-dpr-tools-api-probation
          env: probation-prod
      - build_multiplatform_docker_job:
          name: build_docker
          filters:
            branches:
              only:
                - main
                - tools-probation
          requires:
            - helm_lint
            - helm_lint_probation
            - helm_lint_probation_test
            - helm_lint_probation_preprod
            - helm_lint_probation_prod
            - validate
      - hmpps/deploy_env:
          name: deploy_dev
          env: "dev"
          jira_update: true
          release_name: PROJECT_NAME_ENV_NAME
          context:
            - hmpps-common-vars
            - hmpps-dpr-tools-api-dev
          filters:
            branches:
              only:
                - main
                - tools-probation
          requires:
            - validate
            - build_docker
            - helm_lint
          helm_timeout: 5m
      # Probation API deployment to dev (shares namespace with probation UI)
      - hmpps/deploy_env:
          name: deploy_probation_dev
          env: "probation-dev"
          chart_name: hmpps-dpr-tools-api-probation
          release_name: hmpps-dpr-tools-api-probation
          context:
            - hmpps-common-vars
            - hmpps-dpr-tools-ui-dev-probation
          filters:
            branches:
              only:
                - main
          requires:
            - validate
            - build_docker
            - helm_lint_probation
          helm_timeout: 5m
      - request-test-approval:
          type: approval
          requires:
            - deploy_dev
      - request-probation-test-approval:
          type: approval
          requires:
            - deploy_probation_dev
      - remember-app-version:
          name: remember-version-for-test
          requires:
            - request-test-approval
      - remember-app-version:
          name: remember-version-for-probation-test
          requires:
            - request-probation-test-approval
      - hmpps/deploy_env:
          name: deploy_test
          env: "test"
          jira_update: true
          release_name: PROJECT_NAME_ENV_NAME
          context:
            - hmpps-common-vars
            - hmpps-dpr-tools-api-test
          requires:
            - remember-version-for-test
          helm_timeout: 5m
      - hmpps/deploy_env:
          name: deploy_probation_test
          env: "probation-test"
          chart_name: hmpps-dpr-tools-api-probation
          release_name: hmpps-dpr-tools-api-probation
          jira_update: true
          context:
            - hmpps-common-vars
            - hmpps-dpr-tools-ui-test-probation
          requires:
            - remember-version-for-probation-test
          helm_timeout: 5m
      - request-preprod-approval:
          type: approval
          requires:
            - deploy_dev
      - remember-app-version:
          name: remember-version-for-preprod
          requires:
            - request-preprod-approval
      - hmpps/deploy_env:
          name: deploy_preprod
          env: "preprod"
          jira_update: true
          jira_env_type: staging
          release_name: PROJECT_NAME_ENV_NAME
          context:
            - hmpps-common-vars
            - hmpps-dpr-tools-api-preprod
          requires:
            - remember-version-for-preprod
          helm_timeout: 5m
      - request-probation-preprod-approval:
          type: approval
          requires:
            - deploy_probation_dev
      - remember-app-version:
          name: remember-version-for-probation-preprod
          requires:
            - request-probation-preprod-approval
      - hmpps/deploy_env:
          name: deploy_probation_preprod
          env: "probation-preprod"
          chart_name: hmpps-dpr-tools-api-probation
          release_name: hmpps-dpr-tools-api-probation
          jira_update: true
          jira_env_type: staging
          context:
            - hmpps-common-vars
            - hmpps-dpr-tools-ui-preprod-probation
          requires:
            - remember-version-for-probation-preprod
          helm_timeout: 5m
      - request-prod-approval:
          type: approval
          requires:
            - deploy_preprod
      - remember-app-version:
          name: remember-version-for-prod
          requires:
            - request-prod-approval
      - hmpps/deploy_env:
          name: deploy_prod
          env: "prod"
          jira_update: true
          jira_env_type: production
          slack_notification: true
          slack_channel_name: << pipeline.parameters.releases-slack-channel >>
          release_name: PROJECT_NAME_ENV_NAME
          context:
            - hmpps-common-vars
            - hmpps-dpr-tools-api-prod
          requires:
            - remember-version-for-prod
          helm_timeout: 5m
      - request-probation-prod-approval:
          type: approval
          requires:
            - deploy_probation_preprod
      - remember-app-version:
          name: remember-version-for-probation-prod
          requires:
            - request-probation-prod-approval
      - hmpps/deploy_env:
          name: deploy_probation_prod
          env: "probation-prod"
          chart_name: hmpps-dpr-tools-api-probation
          release_name: hmpps-dpr-tools-api-probation
          jira_update: true
          jira_env_type: production
          slack_notification: true
          slack_channel_name: << pipeline.parameters.releases-slack-channel >>
          context:
            - hmpps-common-vars
            - hmpps-dpr-tools-ui-prod-probation
          requires:
            - remember-version-for-probation-prod
          helm_timeout: 5m
